<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExtendedRealityProject"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".camera.RegistrationActivity"
            android:exported="false"
            android:theme="@style/Theme.ExtendedRealityProject" />
        <activity
            android:name=".camera.GenerationProgressActivity"
            android:exported="false"
            android:theme="@style/Theme.ExtendedRealityProject" />
        <activity
            android:name=".camera.CameraCaptureActivity"
            android:exported="false"
            android:theme="@style/Theme.ExtendedRealityProject" />
        <activity
            android:name=".camera.ModelViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.ExtendedRealityProject" />
        <activity
            android:name=".camera.LocalModelsActivity"
            android:exported="false"
            android:theme="@style/Theme.ExtendedRealityProject" />

        <activity
            android:name=".ui.detail.ItemDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.ExtendedRealityProject" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ExtendedRealityProject">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>